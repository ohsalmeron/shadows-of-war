# OpenSMTPD outbound relay configuration
# Securely restricted to GCP mail server IP

table allowed_ips { __GCP_MAIL_SERVER_IP__ }

listen on 0.0.0.0 port 587

action "relay" relay

match from src <allowed_ips> for any action "relay"
