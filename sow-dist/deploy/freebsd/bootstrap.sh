#!/bin/sh
# RETIRED: historical zroot/Azure FreeBSD bootstrap.
#
# The live host is IONOS with the existing ztmp layout. This script is
# intentionally fail-closed; application deployment belongs to ./sow p.
echo "ERROR: retired Azure FreeBSD bootstrap; use current IONOS tooling and ./sow p" >&2
exit 1
