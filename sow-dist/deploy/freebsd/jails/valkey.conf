valkey {
    host.hostname = "valkey.local";
    path = /zroot/jails/valkey;
    ip4 = inherit;
    ip6 = inherit;
    mount.devfs;
    devfs_ruleset = 4;
    mount.fstab = "/etc/fstab.valkey";
    exec.clean;
    exec.start = "/bin/sh /etc/rc";
    exec.stop = "/bin/sh /etc/rc.shutdown jail";
    allow.set_hostname = 0;
    allow.sysvipc = 0;
    enforce_statfs = 2;
}
